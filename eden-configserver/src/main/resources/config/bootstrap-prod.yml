spring:
  cloud:
    config:
      profile: ${spring.profiles.active}
  rabbitmq:
    host: localhost
    port: 5672
    username: eden
    password: eden
  security:
    user:
      name: eden
      password: eden
      roles: ADMIN

spring:
    cloud:
        bus:
            refresh:
                enabled: true
        config:
            profile: ${spring.profiles.active}
    rabbitmq:
        host: localhost
        port: 5672
        username: admin
        password: DevOps@303

